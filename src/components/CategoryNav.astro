---
import { getAllCategories } from '@/data/categories';
import { blog, category as categoryRoute } from '@/lib/routes';

interface Props {
  activeCategory?: string;
}

const { activeCategory } = Astro.props;
const categories = getAllCategories();
---

<nav class="flex flex-wrap gap-2" aria-label="Blog categories">
  <a
    href={blog()}
    class:list={['badge', !activeCategory ? 'badge-active' : 'hover:border-brand-primary/40 hover:text-brand-primary']}
  >
    All
  </a>
  {
    categories.map((item) => (
      <a
        href={categoryRoute(item.slug)}
        class:list={[
          'badge',
          activeCategory === item.slug
            ? 'badge-active'
            : 'hover:border-brand-primary/40 hover:text-brand-primary',
        ]}
      >
        {item.title}
      </a>
    ))
  }
</nav>
